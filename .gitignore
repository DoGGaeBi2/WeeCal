# IDE (VS Code 등) 설정 파일
.vscode/

# 라이브러리 (용량이 커서 올리면 안 돼)
node_modules/

# 빌드 결과물
dist/

# [가장 중요] 환경 변수 파일 (보안!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 설정 파일
.DS_Store
Thumbs.db